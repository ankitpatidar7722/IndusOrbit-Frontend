diff --git a/node_modules/indas-ui/dist/index.cjs b/node_modules/indas-ui/dist/index.cjs
index 99c0137..78142c9 100644
--- a/node_modules/indas-ui/dist/index.cjs
+++ b/node_modules/indas-ui/dist/index.cjs
@@ -3168,7 +3168,7 @@ function SingleSelect({
       onOpenChange(false);
     }
   }, [filteredOptions, highlightedIndex, searchTerm, onValueChange, onOpenChange, handleCreateOption]);
-  if (allowCustomInput) {
+  if (allowCustomInput || searchable) {
     return /* @__PURE__ */ jsxRuntime.jsxs(Popover__namespace.Root, { open, onOpenChange, children: [
       /* @__PURE__ */ jsxRuntime.jsx(Popover__namespace.Trigger, { asChild: true, children: /* @__PURE__ */ jsxRuntime.jsxs(
         "div",
@@ -3213,8 +3213,13 @@ function SingleSelect({
           collisionPadding: 16,
           onOpenAutoFocus: (e) => {
             e.preventDefault();
-            setTimeout(() => searchInputRef.current?.focus(), 50);
+            const focusSearch = () => searchInputRef.current?.focus();
+            requestAnimationFrame(() => { focusSearch(); setTimeout(focusSearch, 60); });
           },
+          // Mobile: tapping the search box opens the phone keyboard, whose focus change makes Radix
+          // dismiss the popover (the reported "keyboard doesn't show + dropdown closes"). Prevent the
+          // focus-driven dismiss on touch; a genuine tap OUTSIDE still closes it (pointer path).
+          onFocusOutside: (e) => { if (typeof window !== "undefined" && ("ontouchstart" in window || navigator.maxTouchPoints > 0)) e.preventDefault(); },
           children: [
             /* @__PURE__ */ jsxRuntime.jsx("div", { className: "px-3 py-2 border-b border-[rgb(var(--bd-default))]", children: /* @__PURE__ */ jsxRuntime.jsxs("div", { className: "flex items-center gap-2 px-2 py-1.5 rounded-lg border border-[rgb(var(--bd-default))] bg-[rgb(var(--bg-surface))]", children: [
               /* @__PURE__ */ jsxRuntime.jsx(lucideReact.Search, { className: "w-3.5 h-3.5 text-[rgb(var(--fg-muted))] flex-shrink-0" }),
@@ -3247,7 +3252,7 @@ function SingleSelect({
                 className: dropdownSharedStyles2.viewport,
                 onWheel: (e) => e.stopPropagation(),
                 children: [
-                  searchTerm.trim() && !filteredOptions.find((o) => String(o.label ?? "").toLowerCase() === searchTerm.trim().toLowerCase()) && /* @__PURE__ */ jsxRuntime.jsxs(
+                  effectiveCreateOption && searchTerm.trim() && !filteredOptions.find((o) => String(o.label ?? "").toLowerCase() === searchTerm.trim().toLowerCase()) && /* @__PURE__ */ jsxRuntime.jsxs(
                     "button",
                     {
                       type: "button",
@@ -4866,6 +4871,10 @@ function Dropdown({
             sticky: "always",
             collisionPadding: 16,
             onOpenAutoFocus: (e) => e.preventDefault(),
+            // Mobile: tapping the search box opens the phone keyboard, whose focus change makes
+            // Radix dismiss the popover (search box "closes, keyboard never shows"). Stop the
+            // focus-driven dismiss on touch; a real tap OUTSIDE still closes it (pointer path).
+            onFocusOutside: (e) => { if (typeof window !== "undefined" && ("ontouchstart" in window || navigator.maxTouchPoints > 0)) e.preventDefault(); },
             children: /* @__PURE__ */ jsxRuntime.jsx(
               MultiSelect,
               {
@@ -5669,7 +5678,7 @@ function DatePicker({
     return returnFormat === "string" ? getLocalDateString(date) : date;
   };
   React26__namespace.useEffect(() => {
-    if (mode === "single" && !value && onChange) {
+    if (false && mode === "single" && !value && onChange) {
       const now = /* @__PURE__ */ new Date();
       const today = new Date(now.getFullYear(), now.getMonth(), now.getDate(), 0, 0, 0, 0);
       onChange(today);
@@ -7691,7 +7700,7 @@ var SidebarPreferencesContext = React26.createContext(void 0);
 function useSidebarPreferences() {
   const context = React26.useContext(SidebarPreferencesContext);
   if (context === void 0) {
-    throw new Error("useSidebarPreferences must be used within a SidebarPreferencesProvider");
+    return { favorites: [], hiddenItems: [], isFavorite: () => false, isHidden: () => false, toggleFavorite: () => {}, hideItem: () => {}, showItem: () => {}, unhideItem: () => {}, clearHidden: () => {} };
   }
   return context;
 }
@@ -7940,6 +7949,11 @@ var QUICK_CREATE_ROUTES = /* @__PURE__ */ new Set([
 ]);
 var getGroupIcon = (groupName) => {
   const name = groupName.toLowerCase();
+  if (name.includes("overview")) return lucideReact.Gauge;
+  if (name.includes("point management") || name.includes("point tool")) return lucideReact.Target;
+  if (name.includes("module group") || name.includes("authority")) return lucideReact.Layers;
+  if (name.includes("admin")) return lucideReact.Shield;
+  if (name.includes("customer")) return lucideReact.Building;
   if (name.includes("dashboard") || name.includes("home")) {
     return lucideReact.Gauge;
   } else if (name.includes("sales")) {
@@ -8052,7 +8066,7 @@ function DynamicSidebar({
       const cacheKey = `nav_${companyId}_${userId}_${sessionKey}`;
       const cachedNav = localStorage.getItem(cacheKey);
       const cacheTime = localStorage.getItem(`${cacheKey}_time`);
-      if (cachedNav && cacheTime && Date.now() - parseInt(cacheTime) < 3e5) {
+      if (cachedNav && cacheTime && Date.now() - parseInt(cacheTime) < 0 /* Indus: disable 5-min menu cache — always refetch so module-authority changes reflect on refresh */) {
         try {
           const navigation = JSON.parse(cachedNav);
           setGroupedModules(navigation);
diff --git a/node_modules/indas-ui/dist/index.js b/node_modules/indas-ui/dist/index.js
index 920f23c..2d1827b 100644
--- a/node_modules/indas-ui/dist/index.js
+++ b/node_modules/indas-ui/dist/index.js
@@ -1109,7 +1109,7 @@ function SingleSelect({
       onOpenChange(false);
     }
   }, [filteredOptions, highlightedIndex, searchTerm, onValueChange, onOpenChange, handleCreateOption]);
-  if (allowCustomInput) {
+  if (allowCustomInput || searchable) {
     return /* @__PURE__ */ jsxs(Popover.Root, { open, onOpenChange, children: [
       /* @__PURE__ */ jsx(Popover.Trigger, { asChild: true, children: /* @__PURE__ */ jsxs(
         "div",
@@ -1145,7 +1145,11 @@ function SingleSelect({
       /* @__PURE__ */ jsx(Popover.Portal, { container: typeof document !== "undefined" ? document.body : void 0, children: /* @__PURE__ */ jsxs(
         Popover.Content,
         {
-          className: cn(getDropdownClassName2.content(), "flex flex-col !max-h-72 overflow-hidden"),
+          className: cn(getDropdownClassName2.content(), "indas-mpop", "flex flex-col !max-h-72 overflow-hidden"),
+          // The shared content class uses `w-auto`, which collapses to ~0 inside a Radix Popover
+          // portal (unlike Radix Select, Popover doesn't inherit the trigger width) → the open
+          // dropdown is invisible ("not opening"). Force a definite width via inline style.
+          style: { minWidth: "min(320px, 92vw)", maxWidth: "92vw" },
           side: "bottom",
           align: "start",
           sideOffset: 4,
@@ -1154,8 +1158,17 @@ function SingleSelect({
           collisionPadding: 16,
           onOpenAutoFocus: (e) => {
             e.preventDefault();
-            setTimeout(() => searchInputRef.current?.focus(), 50);
+            // On touch devices, DON'T auto-focus the search input: focusing it pops the phone keyboard,
+            // whose viewport resize makes Radix dismiss the popover — so the combobox appears to open
+            // then instantly close. The user taps the search box to type instead.
+            if (typeof window !== "undefined" && ("ontouchstart" in window || navigator.maxTouchPoints > 0)) return;
+            const focusSearch = () => searchInputRef.current?.focus();
+            requestAnimationFrame(() => { focusSearch(); setTimeout(focusSearch, 60); });
           },
+          // Mobile: tapping the search box opens the phone keyboard, whose focus change makes Radix
+          // dismiss the popover (the reported "keyboard doesn't show + dropdown closes"). Prevent the
+          // focus-driven dismiss on touch; a genuine tap OUTSIDE still closes it (pointer path).
+          onFocusOutside: (e) => { if (typeof window !== "undefined" && ("ontouchstart" in window || navigator.maxTouchPoints > 0)) e.preventDefault(); },
           children: [
             /* @__PURE__ */ jsx("div", { className: "px-3 py-2 border-b border-[rgb(var(--bd-default))]", children: /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-2 px-2 py-1.5 rounded-lg border border-[rgb(var(--bd-default))] bg-[rgb(var(--bg-surface))]", children: [
               /* @__PURE__ */ jsx(Search, { className: "w-3.5 h-3.5 text-[rgb(var(--fg-muted))] flex-shrink-0" }),
@@ -1169,7 +1182,8 @@ function SingleSelect({
                   value: searchTerm,
                   onChange: (e) => setSearchTerm(e.target.value),
                   onKeyDown: handleComboKeyDown,
-                  autoFocus: true
+                  // Don't autofocus on touch (keyboard would pop & close the popover); desktop keeps it.
+                  autoFocus: !(typeof window !== "undefined" && ("ontouchstart" in window || navigator.maxTouchPoints > 0))
                 }
               ),
               filteredOptions.length > 0 && /* @__PURE__ */ jsxs("span", { className: "text-[0.625rem] font-medium text-[rgb(var(--fg-muted))] bg-[rgb(var(--bg-subtle))] px-1.5 py-0.5 rounded-md flex-shrink-0 tabular-nums", children: [
@@ -1188,7 +1202,7 @@ function SingleSelect({
                 className: dropdownSharedStyles2.viewport,
                 onWheel: (e) => e.stopPropagation(),
                 children: [
-                  searchTerm.trim() && !filteredOptions.find((o) => String(o.label ?? "").toLowerCase() === searchTerm.trim().toLowerCase()) && /* @__PURE__ */ jsxs(
+                  effectiveCreateOption && searchTerm.trim() && !filteredOptions.find((o) => String(o.label ?? "").toLowerCase() === searchTerm.trim().toLowerCase()) && /* @__PURE__ */ jsxs(
                     "button",
                     {
                       type: "button",
@@ -1293,6 +1307,11 @@ function SingleSelect({
             avoidCollisions: true,
             sticky: "always",
             collisionPadding: 16,
+            // Don't let Radix Select's focus scope grab focus when the searchable content opens /
+            // closes — on mobile that focus-fight closes the search box's keyboard (and the dropdown)
+            // the moment the user taps the search input. Prevent it so the search input keeps focus.
+            onOpenAutoFocus: searchable ? (e) => e.preventDefault() : void 0,
+            onCloseAutoFocus: searchable ? (e) => e.preventDefault() : void 0,
             children: [
               searchable && /* @__PURE__ */ jsx(
                 DropdownSearch,
@@ -2463,6 +2482,10 @@ function DropdownSearch({ searchTerm, setSearchTerm, placeholder = "Type to sear
         className: "w-full text-xs bg-transparent text-[rgb(var(--fg-default))] placeholder:text-[rgb(var(--fg-muted))] focus:outline-none",
         value: searchTerm,
         onChange: (e) => setSearchTerm(e.target.value),
+        onPointerDown: (e) => e.stopPropagation(),
+        onMouseDown: (e) => e.stopPropagation(),
+        onTouchStart: (e) => e.stopPropagation(),
+        onClick: (e) => e.stopPropagation(),
         onKeyDown: (e) => {
           if (stopAllKeyPropagation) {
             if (e.key !== "Escape") {
@@ -2798,8 +2821,11 @@ function Dropdown({
             ref: dropdownContentRef,
             className: cn(
               getDropdownClassName2.content(),
+              "indas-mpop",
               "flex flex-col max-h-60"
             ),
+            // w-auto collapses inside a Radix Popover portal → force a definite width (see combobox note).
+            style: { minWidth: "min(320px, 92vw)", maxWidth: "92vw" },
             side: "bottom",
             align: "start",
             sideOffset: 4,
@@ -2807,6 +2833,10 @@ function Dropdown({
             sticky: "always",
             collisionPadding: 16,
             onOpenAutoFocus: (e) => e.preventDefault(),
+            // Mobile: tapping the search box opens the phone keyboard, whose focus change makes
+            // Radix dismiss the popover (search box "closes, keyboard never shows"). Stop the
+            // focus-driven dismiss on touch; a real tap OUTSIDE still closes it (pointer path).
+            onFocusOutside: (e) => { if (typeof window !== "undefined" && ("ontouchstart" in window || navigator.maxTouchPoints > 0)) e.preventDefault(); },
             children: /* @__PURE__ */ jsx(
               MultiSelect,
               {
@@ -3610,7 +3640,7 @@ function DatePicker({
     return returnFormat === "string" ? getLocalDateString(date) : date;
   };
   React26.useEffect(() => {
-    if (mode === "single" && !value && onChange) {
+    if (false && mode === "single" && !value && onChange) {
       const now = /* @__PURE__ */ new Date();
       const today = new Date(now.getFullYear(), now.getMonth(), now.getDate(), 0, 0, 0, 0);
       onChange(today);
@@ -4315,6 +4345,11 @@ function DatePicker({
           placeholder: getPlaceholder(),
           disabled,
           error,
+          // On touch devices make the field read-only so tapping it opens ONLY the calendar
+          // popover — not the phone's text keyboard, which otherwise collides with / hides the
+          // calendar. Desktop keeps typing enabled.
+          readOnly: typeof window !== "undefined" && ("ontouchstart" in window || navigator.maxTouchPoints > 0),
+          inputMode: (typeof window !== "undefined" && ("ontouchstart" in window || navigator.maxTouchPoints > 0)) ? "none" : void 0,
           className: cn(
             "pl-2 pr-9 text-xs w-full",
             error && "border-red-300"
@@ -4329,6 +4364,7 @@ function DatePicker({
         {
           ref: calendarRef,
           className: cn(
+            "indas-mpop",
             "z-[300] bg-[rgb(var(--bg-surface))] rounded-xl shadow-2xl border border-[rgb(var(--bd-default))] backdrop-blur-sm",
             "data-[state=open]:animate-in data-[state=open]:fade-in-0 data-[state=open]:zoom-in-95",
             "data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=closed]:zoom-out-95",
@@ -4352,7 +4388,13 @@ function DatePicker({
             }
           },
           style: {
-            boxShadow: "0 25px 50px -12px rgba(0, 0, 0, 0.25), 0 0 0 1px rgba(255, 255, 255, 0.05)"
+            boxShadow: "0 25px 50px -12px rgba(0, 0, 0, 0.25), 0 0 0 1px rgba(255, 255, 255, 0.05)",
+            // The Tailwind width class (w-[95vw]) does not take effect inside the Radix popover wrapper,
+            // so the content collapses to ~26px and the 7-col day grid overlaps ("gis pis"). Force a
+            // definite width via inline style (always wins) so the day grid has room. Range mode needs
+            // two months → wider.
+            minWidth: mode === "range" ? "min(580px, 94vw)" : "min(320px, 94vw)",
+            maxWidth: "94vw"
           },
           children: [
             /* @__PURE__ */ jsxs("div", { className: "flex items-center justify-between mb-3", children: [
@@ -5632,7 +5674,7 @@ var SidebarPreferencesContext = createContext(void 0);
 function useSidebarPreferences() {
   const context = useContext(SidebarPreferencesContext);
   if (context === void 0) {
-    throw new Error("useSidebarPreferences must be used within a SidebarPreferencesProvider");
+    return { favorites: [], hiddenItems: [], isFavorite: () => false, isHidden: () => false, toggleFavorite: () => {}, hideItem: () => {}, showItem: () => {}, unhideItem: () => {}, clearHidden: () => {} };
   }
   return context;
 }
@@ -5881,6 +5923,11 @@ var QUICK_CREATE_ROUTES = /* @__PURE__ */ new Set([
 ]);
 var getGroupIcon = (groupName) => {
   const name = groupName.toLowerCase();
+  if (name.includes("overview")) return Gauge;
+  if (name.includes("point management") || name.includes("point tool")) return Target;
+  if (name.includes("module group") || name.includes("authority")) return Layers;
+  if (name.includes("admin")) return Shield;
+  if (name.includes("customer")) return Building;
   if (name.includes("dashboard") || name.includes("home")) {
     return Gauge;
   } else if (name.includes("sales")) {
@@ -5993,7 +6040,7 @@ function DynamicSidebar({
       const cacheKey = `nav_${companyId}_${userId}_${sessionKey}`;
       const cachedNav = localStorage.getItem(cacheKey);
       const cacheTime = localStorage.getItem(`${cacheKey}_time`);
-      if (cachedNav && cacheTime && Date.now() - parseInt(cacheTime) < 3e5) {
+      if (cachedNav && cacheTime && Date.now() - parseInt(cacheTime) < 0 /* Indus: disable 5-min menu cache — always refetch so module-authority changes reflect on refresh */) {
         try {
           const navigation = JSON.parse(cachedNav);
           setGroupedModules(navigation);
@@ -10119,6 +10166,7 @@ function ModalAlert({
       DialogPrimitive.Content,
       {
         className: cn(
+          "indas-alert",
           "fixed left-[50%] top-[50%] z-[201] grid w-[calc(100%-2rem)] max-w-md translate-x-[-50%] translate-y-[-50%] border shadow-2xl rounded-2xl overflow-hidden",
           colors.bg,
           colors.border,
